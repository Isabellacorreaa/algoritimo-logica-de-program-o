// JOIN

const nomes = ["Isabella", "Luana", "Urias", "Ana"];

//console.log(nomes);

//const listaNomes = nomes.join(', ');

//console.log(listaNomes);

//idexOf
const posicaoItem = nomes.indexOf("Luana")
console.log(posicaoItem);

const numeros = [10, 20, 30, 100];

const posicaoNumeros = numeros.indexOf(28)
console.log(posicaoNumeros);
