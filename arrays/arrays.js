//Arrays - São coleção de dados

const numeros = [1, 2, 10, 300, 'Isabella'];

//                 0      1      2
const nomes = ['Isabella', 'Luana', 'Yara', 'Matheus'];

console.log(nomes[1]);

console.log(nomes.length);
